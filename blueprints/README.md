# Blueprints

Blueprints describe reusable abstract structures independent of output syntax.

A blueprint should declare required semantic capabilities rather than naming a target format where possible. Capability matching can then select a compatible representation profile and transformer.

id: transform-engine.declarative-mapping
kind: transform-engine
version: 0.1.0
status: implemented-experimental
title: Repository Declarative Mapping Engine
inspired_by: [XSLT, schema-mapping, compiler-IR-lowering]
implementation: tooling/blueprint_engine/src/blueprint_engine/declarative.py
phases: [select, match, emit]
rule_fields: [select, match, emit_to, emit]
requirements: [deterministic-by-default, explicit-lossiness, profile-aware, validator-hook, provenance-output]
example: transforms/engines/declarative-mapping/example.yaml

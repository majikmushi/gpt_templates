id: format.plantuml
kind: format-family
version: 0.2.0
status: experimental
title: PlantUML
semantic_strictness: semi-formal
role: textual UML-oriented diagram DSL and rendering target
capabilities_seed: [class, sequence, state, activity, component, deployment, object, use-case, package, notes, styles, stereotypes]
representation_bindings: representation-bindings/registry.yaml
format_versions: format-versions/registry.yaml
renderer_compatibility: renderer-compatibility/registry.yaml
validator: validator.plantuml
normative_rule_status: planned-source-and-documentation-derived
notes:
  - No exact PlantUML format release or renderer release is currently pinned; compatibility remains unverified until evidence is registered.

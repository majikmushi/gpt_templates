id: matrix.format-capabilities
kind: capability-matrix
version: 0.3.0
status: experimental
title: Representative Format Capability Matrix
legend: {native: direct semantic support, bound: support through an explicit representation or style binding, partial: incomplete or lossy, none: unsuitable carrier, native-runtime: validated by native runtime}
formats:
  format.uml.class: {typed_entities: native, relationships: native, cardinality: native, behavior: partial, hierarchy: native, visual_overlays: bound, machine_validation: bound, round_trip: bound}
  format.mermaid.class: {typed_entities: native, relationships: native, cardinality: native, behavior: partial, hierarchy: bound, visual_overlays: native, machine_validation: native-runtime, round_trip: bound}
  format.plantuml: {typed_entities: native, relationships: native, cardinality: native, behavior: native, hierarchy: native, visual_overlays: native, machine_validation: bound, round_trip: bound}
  format.json-schema: {typed_entities: bound, relationships: partial, cardinality: bound, behavior: none, hierarchy: native, visual_overlays: none, machine_validation: native, round_trip: bound}
  format.xml: {typed_entities: bound, relationships: partial, cardinality: partial, behavior: none, hierarchy: native, visual_overlays: none, machine_validation: native, round_trip: bound}
  format.markdown: {typed_entities: bound, relationships: partial, cardinality: partial, behavior: partial, hierarchy: native, visual_overlays: bound, machine_validation: partial, round_trip: partial}

# Canonical Models

Canonical models are intermediate semantic representations used to decouple source data from output formats.

Prefer a small shared core plus domain-specific canonical models rather than one universal model that becomes too weakly typed.
